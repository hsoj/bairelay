[default]
all-targets = true
skip-clean = true
# `fail-under = 87` is the honest floor across the whole crate; the
# protocol/server modules individually sit well above it.
fail-under = 87
exclude-files = [
	"docs/*",
	"docs/**/*",
]
